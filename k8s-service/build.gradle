bootJar {
    baseName = 'k8s-service'
    version = "${projectVersion}"
}

dependencies {
    compile('org.springframework.boot:spring-boot-starter-web')
    compile("com.fasterxml.jackson.core:jackson-databind")
}
